# 빌드 산출물 — PyInstaller가 매번 재생성(손으로 커스텀 안 함, SSOT_Explorer.spec도
# 동일 이유로 제외)
build/
dist/
*.spec

__pycache__/
*.pyc
.pytest_cache/

# 2026-08-24 — pytest-cov 실행 산출물(로컬/CI 매 실행마다 재생성)
.coverage
htmlcov/

# D-070 — pyproject.toml 패키징 산출물(pip install -e .가 소스트리 안에 생성)
*.egg-info/

# 개인 로컬 launcher 스크립트(절대경로 하드코딩 — 공개 레포엔 안 올림, 2026-08-15)
scripts/

# 2026-08-24(D-084) — 설계도(결정이력/실행규격서)를 비공개 전환. 다른 모든
# 프로젝트와 동일하게 설계도는 개인 자산으로 로컬 보관만 — GitHub 포트폴리오
# 노출은 README + 다듬어진 웹 아티팩트(해부도/상용화 청사진)로 충분하다고
# 재판단(원본 결정이력의 다듬어지지 않은 방향전환/실패기록까지 낯선 사람에게
# 그대로 노출할 필요는 없음). 이미 push된 과거 이력은 GitHub에 그대로 남음
# (히스토리 재작성은 별도 요청 시에만 — 지금은 향후 추적만 중단).
SSOT_EXP_설계도/
